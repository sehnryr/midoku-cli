[package]
name = "midoku-cli"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.5.8", features = ["derive"] }
midoku-bindings = { path = "../midoku-rs/midoku-bindings" }
miniserde = "0.1.39"
